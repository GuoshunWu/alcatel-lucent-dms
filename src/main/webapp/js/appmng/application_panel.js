// Generated by CoffeeScript 1.5.0
(function() {

  define(['require', 'jqueryui', 'iframetransport', 'iframetransport', 'jqupload', 'i18n!nls/appmng', 'i18n!nls/common', 'util', 'appmng/dialogs', 'appmng/langsetting_grid', 'appmng/stringsettings_grid', 'appmng/history_grid', 'appmng/dictionary_grid'], function(require, $, ifrm, iftrans, jqupload, i18n, c18n, util, dialogs, lgrid, sgrid, hgrid, grid) {
    var appInfo, dctFileUpload,
      _this = this;
    appInfo = {};
    $("#newAppVersion").button({
      text: false,
      label: '&nbsp;',
      icons: {
        primary: "ui-icon-plus"
      }
    }).attr('privilegeName', util.urlname2Action('app/create-application')).click(function(e) {
      return dialogs.newAppVersion.dialog("open");
    });
    $("#removeAppVersion").button({
      text: false,
      label: '&nbsp;',
      icons: {
        primary: "ui-icon-minus"
      }
    }).attr('privilegeName', util.urlname2Action('app/remove-application')).click(function(e) {
      var id;
      id = $("#selAppVersion").val();
      if (!id) {
        return;
      }
      return $.post('app/remove-application', {
        id: id,
        permanent: 'true'
      }, function(json) {
        if (json.status !== 0) {
          $.msgBox(json.message, null, {
            title: c18n.error
          });
          return;
        }
        $("#selAppVersion option:selected").remove();
        return $("#selAppVersion").trigger('change');
      });
    });
    $("#selAppVersion").change(function(e) {
      appInfo.app = {
        version: $("option:selected", this).text(),
        id: this.value ? this.value : -1
      };
      return grid.appChanged(appInfo);
    });
    $("#progressbar").draggable({
      grid: [50, 20],
      opacity: 0.35
    }).progressbar({
      create: function(e, ui) {
        return this.label = $('.progressbar-label', this);
      },
      change: function(e, ui) {
        return this.label.html(($(this).progressbar("value").toPrecision(4)) + "%");
      },
      complete: function(e, ui) {
        return $(this).progressbar("value", 0).hide();
      }
    }).hide();
    dctFileUpload = 'dctFileUpload';
    $('#uploadBrower').button({
      label: i18n.browse
    }).attr('privilegeName', util.urlname2Action('app/deliver-app-dict')).css({
      overflow: 'hidden'
    }).append($("<input type='file' id='" + dctFileUpload + "' name='upload' title='" + i18n.choosefile + "' accept='application/zip' multiple/>").css({
      position: 'absolute',
      top: -3,
      right: -3,
      border: '1px solid',
      borderWidth: '10px 180px 40px 20px',
      opacity: 0,
      filter: 'alpha(opacity=0)',
      cursor: 'pointer'
    }));
    $("#" + dctFileUpload).fileupload({
      type: 'POST',
      dataType: 'json',
      url: "app/deliver-app-dict",
      add: function(e, data) {
        var appId;
        $.each(data.files, function(index, file) {});
        appId = $("#selAppVersion").val();
        if (!appId) {
          return;
        }
        $(this).fileupload('option', 'formData', [
          {
            name: 'appId',
            value: $("#selAppVersion").val()
          }
        ]);
        data.submit();
        if (!$.browser.msie || parseInt($.browser.version.split('\.')[0]) >= 10) {
          $("#progressbar").show();
        }
        return $('#uploadBrower').button('disable');
      },
      progressall: function(e, data) {
        var progress;
        progress = data.loaded / data.total * 100;
        return $('#progressbar').progressbar("value", progress);
      },
      done: function(e, data) {
        var jsonFromServer;
        $('#uploadBrower').button('enable');
        $.each(data.files, function(index, file) {
          return $('#uploadStatus').html("" + file.name + " " + i18n.uploadfinished);
        });
        if (!$.browser.msie || parseInt($.browser.version.split('\.')[0]) >= 10) {
          $("#progressbar").hide();
        }
        jsonFromServer = data.result;
        if (0 !== jsonFromServer.status) {
          $.msgBox(jsonFromServer.message, null, {
            title: c18n.error,
            height: 600,
            width: 800
          });
          return;
        }
        $('#dictListPreviewDialog').data('param', {
          handler: jsonFromServer.filename,
          appId: $("#selAppVersion").val()
        });
        return $('#dictListPreviewDialog').dialog('open');
      }
    });
    return {
      getApplicationSelectOptions: function() {
        return $('#selAppVersion').children('option').clone(true);
      },
      addNewApplication: function(app) {
        return $('#selAppVersion').append("<option value='" + app.id + "' selected>" + app.version + "</option>").trigger('change');
      },
      refresh: function(info) {
        $('#appDispProductName').html(info.parent.text);
        $('#appDispAppName').html(info.text);
        appInfo.base = {
          text: info.text,
          id: info.id
        };
        return $.getJSON("rest/applications/apps/" + info.id, {}, function(json) {
          return $("#selAppVersion").empty().append(util.json2Options(json)).trigger("change");
        });
      }
    };
  });

}).call(this);
