// Generated by CoffeeScript 1.6.2
(function() {
  define(['hchart'], function($) {
    var options, reportChart, showChart;

    options = {
      chart: {
        type: 'pie',
        renderTo: 'chartContainer'
      },
      series: [
        {
          name: 'Total Translations',
          size: '60%',
          dataLabels: {
            color: 'white',
            distance: -30,
            formatter: function() {
              if (this.y > 5) {
                return this.point.name;
              }
              return null;
            }
          }
        }, {
          name: 'Distinct translations',
          size: '80%',
          innerSize: '60%',
          dataLabels: {
            formatter: function() {
              if (this.y > 1) {
                return "<b>" + this.point.name + ": </b> " + this.y;
              }
              return null;
            }
          }
        }
      ],
      title: {
        text: ''
      },
      subtitle: {
        text: ''
      },
      tooltip: {
        valueSuffix: '%'
      },
      plotOptions: {
        pie: {
          shadow: false,
          center: ['50%', '50%']
        }
      }
    };
    reportChart = null;
    showChart = function(title, json, chart) {
      if (chart == null) {
        chart = Highcharts.charts[0];
      }
      if (!json) {
        return;
      }
      if (typeof console !== "undefined" && console !== null) {
        console.log(json);
      }
      options.title.text = title;
      options.subtitle.text = "Translation number: " + json.translationNum;
      options.series[0].data = [['Distinct translations', json.distinctTranslationNum], ['Duplicated translations', json.translationNum - json.distinctTranslationNum]];
      options.series[1].data = [['Translated', json.translatedNum], ['Untranslated', json.untranslatedNum]];
      return reportChart = new Highcharts.Chart(options);
    };
    return {
      showChart: showChart,
      reportChart: reportChart
    };
  });

}).call(this);
